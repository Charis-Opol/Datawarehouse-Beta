# EcoWarehouse — Python dependencies
# Install with:  pip install -r requirements.txt

# Backend API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0       # includes websockets + httptools for speed

# Data warehouse
duckdb>=0.10.0
pandas>=2.0.0
numpy>=1.26.0

# ETL (already used in extract/transform)
requests>=2.31.0
